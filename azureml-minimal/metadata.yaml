Description: |
  A recipe that creates an MLOps stack with an Azure Machine Learning Studio 
  using AzureML as step operator, Azure Blob Storage container as artifact store, 
  Flexible MySQL server as metadata store, and Azure MLflow as experiment tracker.

Cloud: azure

Prerequisites: |
  * The Az CLI on your local machine must be configured with 
    the account you want to deploy resources to. Run `az login`.
