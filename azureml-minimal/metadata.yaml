Description: |
  A recipe that creates an MLOps stack with an Azure Machine Learning Studio 
  using AzureML compute cluster as step operator, Azure Blob Storage container artifact store, 
  Flexible MySQL server as metadata store, and Azure MLflow experiment tracker.

Prerequisites: |
  * The Az CLI on your local machine must be configured with 
    the account you want to deploy resources to. Run `az login`.
